<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Effet au clic -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#FFCC80" /> <!-- Orange clair -->
            <corners android:radius="12dp" /> <!-- 🔹 Réduction de l’arrondi -->
        </shape>
    </item>

    <!-- État normal -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorPrimary" /> <!-- Utilise la couleur du bouton -->
            <corners android:radius="12dp" /> <!-- 🔹 Réduction de l’arrondi -->
            <shadowColor android:color="#99000000"/>
        </shape>
    </item>
</selector>
